<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>SearchEngine BackOffice</title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body>
    <center>
    <div class="btn-group" role="group" aria-label="Basic example">
        <a href="/Tipi_utente"><button type="button" class="btn btn-primary">Tipi_utente</button></a>
        <a href="/Utenti"><button type="button" class="btn btn-primary">Utenti</button></a>
        <a href="/Sessioni"><button type="button" class="btn btn-primary">Sessioni</button></a>
        <a href="/Ricerche"><button type="button" class="btn btn-primary">Ricerche</button></a>
      </div>
    </center>
    <h1><%=tabella%></h1>
    <%if(data){ %>
    <form action="/insert_<%=tabella%>" method="get">
      <% for(let i=0;i<paramaters.length;i++){%>
        <% if((tabella=="Studenti")||(tabella=="Amministratori")){%>
        <input type="text" name="<%= paramaters[i] %>" placeholder="inserisci <%=paramaters[i]%>">
        <% }else if(i>0){%>
        <input type="text" name="<%= paramaters[i] %>" placeholder="inserisci <%=paramaters[i]%>">
        <% } %>
      <% } %>
      <input type="submit" value="Inserisci">
    </form>
    <table class="table">
        <thead>
          <tr>
            <% for(let i=0;i<paramaters.length;i++){%>
            <th scope="col"><%= paramaters[i] %></th>
            <% } %>
          </tr>
        </thead>
        <tbody>
    <% for(let i=0;i<data.length;i++){%>
        <tr>
            <% for(let x=0;x<paramaters.length;x++){%>
                <td><%=data[i][paramaters[x]]%></td>
            <% } %>
            <td><button><a href="/drop_<%=tabella%>?id=<%=data[i][paramaters[0]]%>">Elimina</a></button></td>
        </tr>
    <% } %>
        </tbody>
    </table>
  <% }else{ %>
    <center><h1 style="font-size: 50px;">Tabella vuota</h1></center>
  <% } %>
</body>

</html>